md-card(layout-margin layout-padding flex="")
	.md-title(flex="") {{auth.client}} requests the following privileges:
	ul TODO add oauth2 privilege scopes
		li scopes
		li scopes
	form(action="/api/decision" method="post" target="_top")
			input(name="transaction_id" value="{{auth.id}}" ng-hide="true")
			input(type="submit" value="Allow" id="allow")
			input(type="submit" value="Deny" name="cancel" id="deny")
